# Static services list
# Note: Containers discovered via Docker labels will dynamically populate or merge into these groups.
- Infrastructure:
    - Ubuntu Host:
      - href: "ssh://<server-ip>" # Replace with actual Ubuntu Server IP
      - icon: ubuntu
      - description: "Base OS Ubuntu Server"

- Networking:
    - Local Gateway:
      - href: "http://<router-ip>" # Replace with actual router IP
      - icon: opnsense
      - description: "OPNsense / PFSense Firewall Router"
